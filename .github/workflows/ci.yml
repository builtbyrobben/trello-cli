name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Install dependencies
        run: go mod download

      - name: Run fmt-check
        run: make fmt-check

      - name: Run lint
        run: make lint

      - name: Run tests
        run: make test

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Install dependencies
        run: go mod download

      - name: Build (CGO enabled for keyring)
        run: CGO_ENABLED=1 go build -o bin/trello-cli ./cmd/trello

      - name: Test keyring
        run: |
          echo "testkey" | ./bin/trello-cli auth set-key --stdin
          echo "testtoken" | ./bin/trello-cli auth set-token --stdin
          ./bin/trello-cli auth status

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Install dependencies
        run: go mod download

      - name: Build
        run: go build -o bin/trello-cli.exe ./cmd/trello

      - name: Test keyring
        run: |
          "testkey" | .\bin\trello-cli.exe auth set-key --stdin
          "testtoken" | .\bin\trello-cli.exe auth set-token --stdin
          .\bin\trello-cli.exe auth status
